# Node
node_modules/

# Capacitor web build output (rebuilt in CI)
www/

# Android project (generated fresh in CI from source)
# Uncomment below if you want to exclude android/ from git too.
# Keeping it excluded keeps repo small; CI rebuilds it every time.
android/

# iOS (if added later)
ios/

# Build outputs
*.apk
*.aab
*.keystore
*.jks

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Secrets / env
.env
.env.local
google-services.json
